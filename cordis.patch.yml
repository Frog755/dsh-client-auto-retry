# Bundle patch for @frog755/dsh-client-auto-retry: inserts its plugin row into
# the profile roster. Applied as a profile bundle layer (the dsh.bundle.patch
# manifest field).
- insert:
    - id: auto-retry
      name: '@frog755/dsh-client-auto-retry'
