# Bundle patch for dsh-client-auto-retry: inserts its plugin row into the
# profile roster. Applied as a profile bundle layer (the dsh.bundle.patch
# manifest field).
- insert:
    - id: auto-retry
      name: 'dsh-client-auto-retry'
